# إعداد تسجيل الدخول عبر جوجل باستخدام Firebase

لقد قمت بإضافة الكود اللازم لربط تسجيل الدخول عبر جوجل في تطبيقك (الواجهة الأمامية والخلفية). لإتمام العملية بنجاح، يرجى اتباع الخطوات التالية:

## 1. إعداد الواجهة الأمامية (Frontend)
افتح ملف `src/services/firebase.ts` وقم باستبدال القيم في `firebaseConfig` ببيانات مشروعك التي تحصل عليها من [Firebase Console](https://console.firebase.google.com/):

```typescript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

## 2. إعداد الخلفية (Backend)
للسماح للسيرفر بالتحقق من رموز جوجل، يجب عليك:
1. الذهاب إلى إعدادات المشروع في Firebase Console.
2. الانتقال إلى **Service Accounts**.
3. النقر على **Generate new private key**.
4. قم بتحميل الملف وتسميته بـ `firebase-service-account.json`.
5. ضع هذا الملف داخل مجلد `server/config/` في مشروعك.

## 3. تفعيل طريقة تسجيل الدخول
تأكد من تفعيل **Google** كطريقة تسجيل دخول في قسم **Authentication > Sign-in method** داخل Firebase Console.

## ما تم إنجازه:
- تثبيت حزم `firebase` و `firebase-admin`.
- إنشاء ملفات التكوين في الواجهة الأمامية والخلفية.
- إضافة زر "متابعة باستخدام جوجل" في شاشة تسجيل الدخول.
- إضافة مسار `/api/auth/google` في الخلفية لمعالجة التحقق وإنشاء الحسابات تلقائياً.

بالتوفيق! 🚀
